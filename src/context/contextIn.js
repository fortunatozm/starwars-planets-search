import { createContext } from 'react';

// se nãp recisar usar Provider, basta colocar o valor como parametro no createContext

const InitialContext = createContext();

export default InitialContext;
